services:
  backend:
    build: ./backend
    ports:
      - 3000:3000
  frontend:
    build: ./frontend
    ports:
      - 8080:8080
  redis:
    image: redis:7
    ports:
      - 6379
    volumes:
      - ./redis.conf:/usr/local/etc/redis/redis.conf
